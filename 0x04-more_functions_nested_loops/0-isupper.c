#include <stdio.h>
#include "main.h"

/**
 *_isupper - check main
 *@c: an integer input n
 *Description: function that checks for uppercase character
 *Return: 1 or 0.
 */
int _isupper(int c)

{
	char a;
	int j = 0;

	for (a = 'A'; a <= 'Z'; a++)
		if (a == c)
		{
			j = 0;
			break;
		}
	}
	return (n);
}
